# Copy to .env next to docker-compose.yml. Read by compose, then passed
# through to the coldarr container, then substituted into coldarr.yaml's
# ${VAR} placeholders by Coldarr itself.
TZ=Etc/UTC
RADARR_API_KEY=
SONARR_API_KEY=
JELLYFIN_API_KEY=
